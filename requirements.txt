# ============================================================
# LP-PC Suite — Production Requirements
# ============================================================
# Python 3.11+
# Triết lý: stable-first, performance-where-proven
# ============================================================

# ==================== CORE (BẮT BUỘC) ====================
PyQt6>=6.7.0
PyQt6-Qt6>=6.7.0
PyQt6-sip>=13.8.0
androguard>=4.1.0

# ==================== JSON — TĂNG TỐC ====================
# orjson (Rust): nhanh 3-10x stdlib, drop-in via wrapper
# Fallback: stdlib json (đã có trong smali_utils.py)
orjson>=3.10.0

# ==================== NETWORK ====================
# requests: sync HTTP client
# urllib3: retry adapter
requests>=2.32.0
urllib3>=2.2.0

# Optional — async HTTP (cho downloader, không phải critical path)
# aiohttp>=3.10.0
# httpx>=0.27.0

# ==================== TESTING ====================
pytest>=8.3.0
pytest-cov>=6.0.0
pytest-timeout>=2.4.0
pytest-qt>=4.4.0

# ==================== DEVELOPMENT ====================
black>=24.0.0
ruff>=0.5.0
mypy>=1.10.0

# ============================================================
# OPTIONAL — Chỉ cài nếu THỰC SỰ cần
# ============================================================
# Performance libs dưới đây KHÔNG được code sử dụng mặc định.
# Chỉ thêm khi có profile chứng minh bottleneck cụ thể.

# --- Async event loop (Unix only) ---
# uvloop>=0.20.0; sys_platform != "win32"
# winloop>=0.2.0; sys_platform == "win32"

# --- Regex engine (KHÔNG khuyến nghị) ---
# google-re2 có API khác stdlib re (không có re.DOTALL, thiếu lookahead).
# Chỉ dùng nếu refactor toàn bộ regex sang re2.Options().
# google-re2>=1.1.20240702

# --- Hashing (thay md5 cho security) ---
# blake3>=0.4.0   # Chỉ cần nếu bảo mật, không phải performance

# --- Compression (KHÔNG dùng bởi pipeline hiện tại) ---
# zstandard>=0.23.0
# lz4>=4.3.0
# brotli>=1.1.0

# --- Multi-part JSON ---
# msgspec>=0.21.1   # Chỉ cần khi có typed schema

# --- Nailgun (tăng tốc apktool qua JVM server) ---
# nailgun>=0.9.0    # Cần Java, chạy `java -jar tools/bin/nailgun-server.jar`

# ============================================================
# NOTES
# ============================================================
# 1. re2 không tương thích với code hiện tại — dùng stdlib re.
# 2. orjson đã tích hợp qua wrapper trong smali_utils.py.
# 3. uvloop/winloop chỉ tác dụng nếu pipeline dùng asyncio (hiện tại không).
# 4. blake3 chỉ dùng cho security, không phải performance.
# 5. Không thêm dependency trừ khi có profile chứng minh cần.